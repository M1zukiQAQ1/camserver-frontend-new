export default defineAppConfig({
  ui: {
    colors: {
      // Matches the sky/cyan accents used across the pages (focus rings, primary buttons, links).
      primary: 'sky',
      neutral: 'slate'
    }
  }
})
